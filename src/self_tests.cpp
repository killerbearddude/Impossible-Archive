/*
 * Regression suite dispatcher for invariants across access control, validation,
 * generation, materialization, and formatting.
 *
 * The test body is kept in a separate include file as a mechanical first step
 * toward subsystem-level self-test organization. Keep behavior changes out of
 * this file unless the corresponding included tests are deliberately updated.
 */
#include "self_tests_body.inc"
